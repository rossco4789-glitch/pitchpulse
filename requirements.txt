pandas
mplsoccer
opencv-python        # cv/calibration.py, cv/picker.py — Tier 1 homography (CPU only)
streamlit            # app.py — desktop dashboard (local only, no cloud)
python-docx          # data/parse_report.py — club .docx match reports
kaggle               # tools/cloud_vision_runner.py — headless Kaggle GPU batch runs (CLAUDE.md §5 exception)
